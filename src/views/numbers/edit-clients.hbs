<div class="container p-4">
   <div class="row">
    <div class="col-md-4 mx-auto">
        <div class="card">
            <div class="card-body">
                <form action="/numbers/edit-clients/{{numbers.id}}" method="POST">
                    <p>Ingresa el nombre de la persona que compro este numero</p>
                    <p>{{numbers.NumbersNumber}}</p>
                    <div class="form-label">
                        <input type="text" value= "{{numbers.ClientsName}}" class="form-control" name="ClientsName" placeholder="Nombre" autofocus>
                    </div>
                    <div class="form-label">
                        <input type="text" value= "{{numbers.ClientsEmail}}" class="form-control" name="ClientsEmail" placeholder="Email del comprador" autofocus>
                    </div>
                    <div class="form-label">
                        <input type="text" value= "{{numbers.ClientsPhone}}" class="form-control" name="ClientsPhone" placeholder="Telefono del comprador" autofocus>
                    </div>
                    <div class="form-label">
                        <button class="btn-block btn btn-success">
                            Editar
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
   </div> 
</div>